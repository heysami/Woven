# Playwright + pngjs are installed on demand via Settings → local tools
# (the /__local_install "npm" kind), per daemon location - never committed.
node_modules/

# Direct-download binaries (cloudflared, glslangValidator) installed on demand
# via the /__local_install "binary" kind - pinned official release downloads,
# never committed.
bin/
