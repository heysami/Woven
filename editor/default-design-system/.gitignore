# The committed sample snapshots ARE tracked - the Capabilities → Template
# Design System gallery serves them as plain <img>s. Regenerate them offline
# with the headless boot in scratchpad/dsgen (drives dsInjectSampleHead in a
# 1280x800 iframe per sample+view and writes previews/<id>-<view>.png).
!previews/
