{
  "_intent": "CRAFT-FAIL (permission leak) fixture - runtime calls navigator.mediaDevices.getUserMedia() AT MODULE LOAD with no Start gate. craft-lens §3.1 'Permission requested behind a user gesture' must catch as block-severity. Aesthetic + concept are downstream and irrelevant once craft blocks.",
  "craft": {
    "verdict": "fail",
    "must_cite": ["getUserMedia", "user gesture", "Start"],
    "expected_failure_check": "Permission requested behind a user gesture",
    "expected_severity":      "block",
    "ground_truth_reason": "getUserMedia({audio:true}) is called at top-level module scope, not inside any user-event handler; no Start gate; browser autoplay policy will block AudioContext from running anyway"
  },
  "aesthetic": {
    "verdict": "n/a",
    "skip_reason_hint": "runtime fails to even reach a viewable state due to permission block; aesthetic-lens may pass on the gradient potential or skip - either acceptable"
  },
  "concept": {
    "verdict": "n/a",
    "skip_reason_hint": "runtime cannot be exercised because permission is denied / never granted via Start gate; concept-lens may skip"
  }
}
